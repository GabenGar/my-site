mixin svgIcon(id, className=null)
  span.svg-icon(
    class= className && className
  )
    svg.svg-icon__svg
      use.svg-icon__use(
        href= `<%= require("#assets/svg-spritesheet/_index.svg") %>#svg-icon--${id}`)
